export enum ENUM_POSITION {
    top = 'top',
    center = 'center',
    bottom = 'bottom',
    topRight = 'topRight',
}

export enum PROGRESS {
    left = 'left',
    center = 'center',
    right = 'right',
}
